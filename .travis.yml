language: node_js
node_js:
  - 9

jobs:
  include:
    - stage: Lint
      install: npm install -g npm-audit-resolver
      script:
        - check-audit
        - npm run lint

    # - stage: Unit Tests
    #   script: npm run test

    # - stage: Build Application
    #   script:
    #     - docker build --tag app-nodejs .
