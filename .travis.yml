language: node_js
node_js:
  - 9

jobs:
  include:
    - stage: Audit
      install: npm install
      script:
        - npm audit --parseable | awk -F $'\t' '{print $1,$4}'


    # - stage: Unit Tests
    #   script: npm run test

    # - stage: Build Application
    #   script:
    #     - docker build --tag app-nodejs .
